<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bhaimadadchahiye.club" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".start.SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:label="Bhai Madad Chahiye"
            android:name="com.bhaimadadchahiye.club.start.SplashScr"
            android:theme="@android:style/Theme.WithActionBar">
        </activity>

        <activity android:label="@string/action_sign_in" android:name="com.bhaimadadchahiye.club.login.LoginActivity">
        </activity>

        <activity android:label="@string/create_account" android:name="com.bhaimadadchahiye.club.login.Register">
        </activity>

        <activity android:label="@string/app_name" android:name="com.bhaimadadchahiye.club.login.Registered">
        </activity>

        <activity android:label="Bhai Madad Chahiye User Panel" android:name="com.bhaimadadchahiye.club.login.MyMainActivity">
        </activity>

        <activity android:label="@string/forgot_password" android:name="com.bhaimadadchahiye.club.login.PasswordReset">
        </activity>

        <activity android:label="@string/change_password" android:name="com.bhaimadadchahiye.club.login.ChangePassword">
        </activity>
    </application>

</manifest>
