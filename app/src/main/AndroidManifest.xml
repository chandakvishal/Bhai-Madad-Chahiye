<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bhaimadadchahiye.club">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".start.SplashScreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.bhaimadadchahiye.club.start.SplashScr"
            android:label="Bhai Madad Chahiye"
            android:theme="@android:style/Theme.WithActionBar" />

        <activity
            android:name="com.bhaimadadchahiye.club.login.LoginActivity"
            android:label="@string/action_sign_in" />

        <activity
            android:name="com.bhaimadadchahiye.club.login.Register"
            android:label="@string/create_account" />

        <activity
            android:name="com.bhaimadadchahiye.club.login.Registered"
            android:label="@string/app_name" />

        <activity
            android:name="com.bhaimadadchahiye.club.login.PasswordReset"
            android:label="@string/forgot_password" />

        <activity
            android:name="com.bhaimadadchahiye.club.login.ChangePassword"
            android:label="@string/change_password" />

        <activity
            android:name=".ActualMatter.NavigationMenu.MenuActivity"
            android:label="@string/toolbar" />
    </application>
</manifest>